extends ../layout

block content
    .cards
        each category in categories
            .product-card
                h2
                   a(href='/topic/list/' + category.name)= category.name
